export interface ProfitData {
  NM_CLI: string;
  V_FAT: number;
  Q_FAT: number;
  LUC: number;
}

export interface ChartFilter {
  startDate: string;
  endDate: string;
  maxChartItems: number;
}
